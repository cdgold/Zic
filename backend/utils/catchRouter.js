/*
const catchRouter = require("express").Router()

catchRouter.get('*', function(request, response) {
    response.sendFile(path.join(__dirname, './build/index.html'), function(err) {
      if (err) {
        response.status(500).send(err)
      }
    })
  })

module.exports = catchRouter
*/